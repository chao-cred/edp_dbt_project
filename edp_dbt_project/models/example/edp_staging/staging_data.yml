version: 2

models:
  - name: edp_staging_capital_goods
    columns:
      - name: pk_id
        tests:
          - unique
          - not_null
      - name: account_code
        tests:
          - not_null
  - name: edp_staging_account_mapping
    columns:
      - name: hfm_name
        tests:
          - unique
          - not_null
  - name: edp_staging_org_mapping
    columns:
      - name: hfm_name
        tests:
          - unique
          - not_null
